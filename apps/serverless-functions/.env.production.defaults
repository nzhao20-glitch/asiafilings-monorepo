# .env.production.defaults
# Documents production environment variables — safe to commit.
# Most values are injected by Terraform (lambda.tf / extraction.tf).
# Secrets (DATABASE_URL) are in AWS SSM at /platform/lambda/prod/
# Run: ./tools/scripts/pull-secrets.sh --app lambda --env prod

# ── Downloader Lambda (lambda.tf) ──
# DATABASE_URL      = <Terraform: var.database_url>
# S3_BUCKET         = <Terraform: data.aws_s3_bucket.pdfs.bucket>
# S3_PREFIX         = <Terraform: var.s3_prefix>
# CONCURRENCY       = <Terraform: var.lambda_concurrency>
# PROXY_BASE_URL    = <Terraform: var.proxy_base_url>

# ── Extraction Lambda (extraction.tf) ──
# DATABASE_URL      = <Terraform: var.database_url>
# SOURCE_BUCKET     = <Terraform: data.aws_s3_bucket.pdfs.bucket>
# SOURCE_PREFIX     = <Terraform: var.s3_prefix>
# EXTRACTION_BUCKET = <Terraform: data.aws_s3_bucket.extractions.bucket>
# OUTPUT_PREFIX     = processed
# TABLES_PREFIX     = tables
# MAX_WORKERS       = <Terraform: var.extraction_max_workers>

# ── Shared ──
AWS_REGION=ap-east-1
