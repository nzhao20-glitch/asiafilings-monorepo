# Database connection
# For local SQLite (development):
DATABASE_URL=sqlite:///hkex.db

# For PostgreSQL (production):
# DATABASE_URL=postgres://user:password@host:5432/hkex

# AWS Configuration
AWS_REGION=ap-east-1
SOURCE_BUCKET=hkex-documents
EXTRACTION_BUCKET=hkex-extractions

# Lambda Configuration
MAX_WORKERS=6
